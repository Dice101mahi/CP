#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main(){
    int t, m, n, o, p;

    cin>>t;

    while(t--){
        cin>>m;

        int a[m];
        vector<int> b;
        b.push_back(a[0]);

        for(int i = 0 ; i < m ; i++){
            cin>>a[i];
        }

        for(int i = 1 ; i < m ; i++){
            for(int j = 0 ; j < b.size() ; j++){
                if(b[j] != )
            }
        }
    }

    return 0;
}